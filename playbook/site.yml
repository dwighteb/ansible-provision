---
- hosts: openvpn
  user: "{{ myuser | default('ubuntu') }}"
  sudo: yes
  roles:
    - common
    - openvpn

- hosts: dell
  user: "{{ myuser | default('ubuntu') }}"
  sudo: yes
  roles:
    - common
    - dell
